<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Product List</h1>
        <div class="product-grid">
            <% products.forEach(product => { %>
                <div class="product-card">
                    <div class="product-title"><%= product.productName %></div>
                    <div class="product-category">Category: <%= product.category %></div>
                    <div class="product-price">₹ <%= product.price %></div>
                    <div class="product-quantity">Quantity: <%= product.quantity %></div>
                    <div class="product-fastmoving">
                        Fast Moving: 
                        <span style="color:<%= product.isFastMoving ? '#27ae60' : '#e74c3c' %>;">
                            <%= product.isFastMoving ? "Yes" : "No" %>
                        </span>
                    </div>
                    <div class="button-group">
                        <a href="/products/<%= product._id %>" class="btn edit">Edit</a>
                        <form action="/products/<%= product._id %>?_method=DELETE" method="POST" style="display:inline;">
                            <button type="submit" class="btn delete">Delete</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>